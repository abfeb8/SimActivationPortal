server:
  port: 9070

spring:
  application:
    name: UserMS

  cloud:
    #consul config
    consul:
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefix: config
        defaultContext: application
        profileSeparator: '::'
        format: YAML